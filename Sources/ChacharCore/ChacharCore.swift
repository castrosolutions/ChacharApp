import Foundation

/// ChacharApp core: engine-agnostic transcription, audio capture and correction.
///
/// A pure, testable layer shared by the menubar app (`ChacharApp`) and the
/// measurement spike (`ChacharSpike`).
public enum ChacharCore {
    /// Semantic version of the core module.
    public static let version = "0.0.1"
}
